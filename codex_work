给后续 Codex / AI 代理的说明：本文件是当前仓库的持续工作交接文档。到目前为止，我做过的计划、执行过程、结果、验证证据、当前状态和下一步计划，都持续记录在这里。一个完全没有历史上下文的新 AI，接手时应先读这个文件，再看 `README.md`、`docs/`、`paper/`、`schemas/`、`src/` 和 `tests/`，这样可以最快恢复上下文，避免重复调研、重复搭架子或误判当前进度。

项目名：EvidenceWeaver
本地工作目录：/Users/jasvina/jasvina_github/EvidenceWeaver
远端仓库：git@github.com:Jasvina/EvidenceWeaver.git
主分支：main
项目定位：citation-grounded reinforcement learning for deep research agents

=== 时间线记录 ===

04月20日23时11分
- 工作：完成仓库第一轮公共叙事搭建，写出图文并茂的 `README.md`，补充 `docs/related-work.md`、`docs/research-agenda.md`、`docs/reward-design.md`、`docs/benchmark-slice.md`、`paper/outline.md`，并加入 `docs/assets/banner.svg` 与 `docs/assets/evidence-loop.svg`。
- 结果：仓库从空白页变成一个可以对外介绍的研究项目首页，明确了项目 thesis、路线图、研究问题和早期方向。
- 关联提交：`9559e61`

04月20日23时15分
- 工作：补充 `docs/interfaces.md`，并创建 `.github/ISSUE_TEMPLATE/` 下的 benchmark / reward / failure case issue 模板。
- 结果：仓库从“只有想法”推进到“可以结构化收集协作输入”的状态，后续 benchmark、reward、failure case 可以用 GitHub issue 原生流程沉淀。
- 关联提交：`85bb653`

04月20日23时34分
- 工作：把项目从纯文档仓库推进成可执行的最小研究 scaffold。新增 `pyproject.toml`、`src/evidenceweaver/`、`schemas/`、`examples/`、`tests/`、`paper/draft.md`、`LICENSE`，并更新 `README.md` 与 `docs/benchmark-slice.md`。
- 结果：仓库现在已经有最小可运行骨架：
  - `schemas/task-bundle.v0.json` 与 `schemas/run-artifact.v0.json` 定义了 benchmark 输入输出契约。
  - `src/evidenceweaver/models.py` 提供任务、运行轨迹和评估报告的结构化加载与校验。
  - `src/evidenceweaver/eval/offline.py` 提供离线 evaluator，可对一个 task bundle + run artifact 输出 evidence-centric metrics。
  - `examples/tasks/synthetic_delay_task.json` 与 `examples/runs/*.json` 提供了一个 synthetic snapshot-based benchmark 样例。
  - `tests/test_evaluator.py` 覆盖了解析、CLI 评估和 good run vs weak run 的对比检查。
  - `paper/draft.md` 已经从 outline 扩成 pre-results working draft。
- 关联提交：`22886a6`

04月20日23时36分
- 工作：按用户最新要求，将本地工作目录统一放到 `/Users/jasvina/jasvina_github/EvidenceWeaver`，并新增根目录工作交接文件 `codex_work`。
- 结果：后续任何新 AI 都可以先读这一个文件快速接管，不需要依赖会话历史；本地工作路径也已经与用户指定目录统一。
- 备注：此前仓库曾在 `/Users/jasvina/EvidenceWeaver` 工作，现已迁移到用户指定目录。

=== 当前结果 ===

1. 仓库已经不是空壳：
- 有项目首页与视觉资产。
- 有相关工作、研究议程、reward 设计、benchmark slice、接口草案。
- 有 pre-results paper draft。
- 有最小 Python 包、schema、example artifacts 和 tests。

2. 当前已经落地的关键文件：
- `README.md`
- `docs/related-work.md`
- `docs/research-agenda.md`
- `docs/reward-design.md`
- `docs/benchmark-slice.md`
- `docs/interfaces.md`
- `paper/outline.md`
- `paper/draft.md`
- `schemas/task-bundle.v0.json`
- `schemas/run-artifact.v0.json`
- `src/evidenceweaver/models.py`
- `src/evidenceweaver/eval/offline.py`
- `tests/test_evaluator.py`

3. 当前 evaluator 输出的核心指标包括：
- `answer_coverage`
- `citation_coverage`
- `citation_precision`
- `chain_completeness`
- `source_diversity`
- `budget_efficiency`
- `unsupported_claim_rate`
- `overall_score`

4. 当前的 benchmark/example 还只是 synthetic v0，不是正式实验 benchmark。
- 这套样例的目的不是拿结果，而是固定接口、固定 artifact 形状、固定测试路径。

=== 验证记录 ===

已完成的本地验证：
- `git diff --check`
- `PYTHONPATH=src python3 -m compileall src`
- `PYTHONPATH=src python3 -m unittest discover -s tests -v`
- `PYTHONPATH=src python3 -m evidenceweaver.eval.offline examples/tasks/synthetic_delay_task.json examples/runs/synthetic_delay_good_run.json`
- 对 `schemas/*.json` 与 `examples/*.json` 进行了 JSON 解析检查

已知验证结果：
- 3 个单元测试通过。
- synthetic good run 在当前 evaluator 下得到满分 `overall_score = 1.0`。
- weak run 分数低于 good run，说明当前 evaluator 至少具备最基本的区分能力。

=== 当前状态判断 ===

当前仓库状态可以概括为：
- 已完成：项目定位、对外叙事、协作入口、v0 schema、v0 evaluator、synthetic example、paper draft。
- 未完成：真实 benchmark slice、真实 agent loop、evidence graph 实现、reward server、训练脚本、ablation、正式实验结果。
- 风险点：
  - evaluator 还是 heuristic，不是 learned reward model。
  - example benchmark 还是 synthetic，尚未绑定真实研究任务数据。
  - `paper/draft.md` 是 pre-results draft，不能误读成已有实验结论的论文。

=== 下一步计划 ===

优先级最高的下一步建议：
1. 在 `examples/` 之外引入第一版更真实的 snapshot-based benchmark slice。
2. 开始实现 `src/evidenceweaver/agent/` 的最小 search-read-write loop。
3. 在 `src/evidenceweaver/` 内加入 evidence graph 数据结构，而不只是文档级 citation 检查。
4. 把 `offline evaluator` 扩成更可解释的 claim-level diagnostics 输出。
5. 让 `paper/draft.md` 与代码结构同步，逐步补成方法、实验、局限性更完整的论文草稿。

如果未来有新的工作，请继续按“时间 + 做了什么 + 结果 + 下一步”的格式追加在本文件末尾。

04月21日00时05分
- 工作：实现了 `src/evidenceweaver/agent/baseline.py` 的最小 search-read-write baseline agent，并新增 `src/evidenceweaver/agent/__init__.py`。这个 agent 能在 snapshot task 上执行搜索、打开文档、抽取 claim、生成带 citation 的 `run-artifact.v0` JSON。
- 结果：仓库不再只有 evaluator，已经具备一个可运行的最小 agent loop，后续可以在此基础上接 evidence graph、reward server 和 RL trainer。

04月21日00时05分
- 工作：新增 `benchmarks/snapshot_v0/README.md` 以及 3 个更真实的 snapshot benchmark task：
  - `agentic_rl_stability_task`
  - `agent_training_stack_task`
  - `deep_search_reward_task`
  这些任务都使用与真实 primary source URL 绑定的 paraphrased snapshot digest。
- 结果：仓库现在同时有 synthetic task 和更真实的 research-style benchmark seed set，benchmark 不再只有玩具数据。

04月21日00时05分
- 工作：扩写 `paper/draft.md`，把原先较短的 pre-results draft 扩成更完整的论文工作草稿，增加了问题陈述、与现有工作的关系、当前仓库 artifact、benchmark 设计、baseline agent、实验议程、风险与局限性等章节。
- 结果：论文材料已经从 outline + 短草稿升级为更接近正式 paper narrative 的版本，后续可以在此基础上继续补方法、实验和结果。

04月21日00时05分
- 工作：更新 `README.md` 与 `docs/benchmark-slice.md`，加入 baseline agent 的运行方式、snapshot benchmark v0 的入口、当前 repo layout 的新目录，以及更真实 benchmark 的说明。
- 结果：GitHub 首页已经同步反映代码与 benchmark 的新进展，不会出现“README 落后于代码”的问题。

04月21日00时05分
- 工作：新增 `tests/test_baseline_agent.py`，并把验证扩展到 benchmark 目录加载、baseline agent CLI 输出、baseline agent 在全部 snapshot_v0 任务上的分数下界检查。
- 结果：现在本地测试已经能覆盖 agent + evaluator + benchmark seed 的完整链路，后续改动更容易被回归测试拦住。

04月21日00时05分
- 验证：
  - `git diff --check`
  - `PYTHONPATH=src python3 -m compileall src`
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.agent.baseline benchmarks/snapshot_v0/tasks/agentic_rl_stability_task.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.eval.offline benchmarks/snapshot_v0/tasks/agentic_rl_stability_task.json /tmp/ew_stability_run.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.agent.baseline benchmarks/snapshot_v0/tasks/agent_training_stack_task.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.eval.offline benchmarks/snapshot_v0/tasks/agent_training_stack_task.json /tmp/ew_stack_run.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.agent.baseline benchmarks/snapshot_v0/tasks/deep_search_reward_task.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.eval.offline benchmarks/snapshot_v0/tasks/deep_search_reward_task.json /tmp/ew_search_run.json`
- 验证结果：
  - 8 个单元测试全部通过。
  - baseline agent 在 `snapshot_v0` 三个任务上的 `overall_score` 都 >= 0.55。
  - 实测分数大致为：
    - stability task: `0.9`
    - training stack task: `0.9625`
    - deep search reward task: `0.9667`

=== 当前状态判断（04月21日更新） ===

当前仓库状态可以概括为：
- 已完成：项目定位、对外叙事、协作入口、v0 schema、v0 evaluator、synthetic example、snapshot_v0 benchmark seed、最小 baseline agent、paper draft。
- 未完成：evidence graph、reward bundle 自动生成、trainer 集成、正式实验、ablation、真实冻结网页 benchmark。
- 当前最重要的价值：仓库已经具备“任务 -> agent -> run artifact -> evaluator -> paper narrative”的最小闭环。
- 当前最主要的风险：
  - benchmark 仍然偏小，且 realistic slice 仍是 paraphrased snapshot，不是高保真 frozen-web snapshot。
  - baseline agent 还是 deterministic heuristic，不代表真正的 RL agent 能力。
  - evaluator 仍是 heuristic evaluator，未来要防止 benchmark / evaluator 被 baseline 过拟合。

=== 更新后的下一步计划 ===

1. 实现 `src/evidenceweaver/graph/`，把 claim-source-support 关系从 evaluator 逻辑外提成显式 evidence graph。
2. 给 baseline agent 增加 graph-aware state，而不是仅靠 sentence extraction 直接写答案。
3. 开始设计 `reward_bundle` 的离线生成逻辑，让 run artifact 可以自动带上 decomposed reward。
4. 进一步扩展 snapshot benchmark，增加更异构的任务类型和更难的 held-out tasks。
5. 继续扩写 `paper/draft.md` 的方法、实验设计和 failure analysis 部分。

04月21日00时15分
- 工作：新增显式 evidence graph 模块 `src/evidenceweaver/graph/`，包括 `EvidenceGraph`、`SourceNode`、`ClaimNode`、`GraphEdge`、`OpenQuestion` 和 `EvidenceGraphBuilder`。
- 结果：仓库不再只是“在 evaluator 里隐式推断证据结构”，而是已经有可以落盘到 run artifact 的显式证据图结构，后续 reward、policy、debug 都可以直接消费这层结构。

04月21日00时15分
- 工作：把 baseline agent 改成 graph-aware loop：
  - 打开文档时标记 source node 为 opened
  - 写 claim 时创建 claim node 和 `supports` edge
  - 根据未覆盖 prompt focus token 自动记录 `open_questions`
  - 生成 run artifact 后立刻调用 evaluator，把 `reward_bundle` 自动附到 run 上
- 结果：EvidenceWeaver 现在已经从“task -> agent -> answer”推进到“task -> agent -> evidence graph -> reward bundle”的更完整闭环。

04月21日00时15分
- 工作：扩展 `run-artifact.v0`，让 schema 支持 `evidence_graph`；同时给 evaluator CLI 增加 `--emit-scored-run`，可以把任意 run artifact 输出为自动附带 `reward_bundle` 的 scored run。
- 结果：后续即使不是 baseline agent 生成的 run，也可以通过 evaluator CLI 统一补齐 reward bundle，artifact contract 更完整了。

04月21日00时15分
- 工作：补充测试，新增对 evidence graph 与 scored run 的检查，包括：
  - baseline agent 会输出 `evidence_graph`
  - baseline agent 会输出 `reward_bundle`
  - evaluator CLI 可以写出带 reward bundle 的 scored run
- 结果：当前测试不只验证 parser / evaluator，还能覆盖 graph-aware baseline 和 reward enrichment 路径。

04月21日00时15分
- 工作：更新 `README.md`、`docs/interfaces.md`、`docs/benchmark-slice.md`、`paper/draft.md`，把 graph module、reward bundle enrichment 和新的闭环流程写进文档与论文草稿。
- 结果：仓库叙事已经与当前代码能力对齐，文档不会落后于实现。

04月21日00时15分
- 验证：
  - `git diff --check`
  - `PYTHONPATH=src python3 -m compileall src`
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.agent.baseline benchmarks/snapshot_v0/tasks/agentic_rl_stability_task.json --output /tmp/ew_graph_run.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.eval.offline benchmarks/snapshot_v0/tasks/agentic_rl_stability_task.json /tmp/ew_graph_run.json --emit-scored-run /tmp/ew_scored_run.json`
- 验证结果：
  - 10 个单元测试全部通过。
  - scored run 中已经稳定包含 `evidence_graph` 与 `reward_bundle`。
  - baseline + evaluator 的闭环验证通过。

=== 更新后的下一步计划（04月21日00时15分） ===

1. 实现 `src/evidenceweaver/graph/` 上层的 graph-aware policy state，而不是只让 baseline 在输出时写图。
2. 开始把 `claim_results` / diagnostics 更系统地挂到 artifact 或 sidecar report 上。
3. 设计真正的 reward decomposition generation pipeline，而不只是从 evaluator metrics 映射到 `RewardBundle`。
4. 扩展 benchmark，加入更复杂、更异构、更接近真实 research workflow 的 snapshot tasks。
5. 在 trainer 选型前，先把 graph + benchmark + reward 的实验合同钉得更牢。

04月21日00时33分
- 工作：继续把 graph 从“只落在 artifact 里”推进到“真正影响 baseline 行为”。现在 baseline agent 会基于 `open_questions` 和未覆盖的 prompt focus token 构造 follow-up search query，并在预算允许时执行第二轮检索与补充 claim。
- 结果：EvidenceWeaver 已经不只是 graph-aware output，而是出现了 graph-driven retrieval refinement。至少在 `snapshot_v0` 任务集上，agent 的第二轮检索路径已经真实发生。

04月21日00时33分
- 工作：新增 `RunDiagnostics`，并把 `diagnostics` 挂到 `run-artifact.v0`。目前包含：
  - `search_queries`
  - `iteration_count`
  - `opened_source_ids`
  - `opened_source_count`
  - `claim_count`
  - `covered_prompt_focus_ratio`
  - `uncovered_focus_tokens`
  - `notes`
- 结果：run artifact 现在不仅有 answer、graph、reward，也有更适合调参与回归分析的 diagnostics 层。

04月21日00时33分
- 工作：更新 schema、模型、baseline agent、测试与文档，使 `diagnostics` 与 iterative follow-up search 成为正式支持的仓库能力。
- 结果：README、interfaces、benchmark 文档和 paper draft 已经同步说明：当前 baseline 支持 graph-driven follow-up iteration，而不只是一次性检索。

04月21日00时33分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.agent.baseline benchmarks/snapshot_v0/tasks/agent_training_stack_task.json --output /tmp/ew_stack_followup.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.eval.offline benchmarks/snapshot_v0/tasks/agent_training_stack_task.json /tmp/ew_stack_followup.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.agent.baseline benchmarks/snapshot_v0/tasks/agentic_rl_stability_task.json --output /tmp/ew_stability_followup.json`
  - `PYTHONPATH=src python3 -m evidenceweaver.eval.offline benchmarks/snapshot_v0/tasks/agentic_rl_stability_task.json /tmp/ew_stability_followup.json`
- 验证结果：
  - 11 个单元测试全部通过。
  - snapshot suite 中至少一个任务已经发生 `iteration_count > 1` 的 follow-up search。
  - `agent_training_stack_task` 当前基线分数约为 `0.6667`。
  - `agentic_rl_stability_task` 当前基线分数约为 `0.9`。

=== 更新后的下一步计划（04月21日00时33分） ===

1. 在 `graph/` 上加入更丰富的 edge / state 语义，例如 contradiction、derived_from、duplicate。
2. 把 diagnostics 从 run artifact 进一步扩到专门的 eval / debug sidecar，支持更系统的 failure analysis。
3. 开始设计独立的 reward generation module，而不是仅由 evaluator report 映射得到 reward bundle。
4. 让 benchmark 中的 task provenance 更标准化，例如加入 source metadata 或 refresh recipe。
5. 在 trainer 集成前继续扩大 graph-aware baseline 与 benchmark 的实验合同。

04月21日00时42分
- 工作：把 reward 逻辑从 evaluator 内部进一步拆成独立模块，新增 `src/evidenceweaver/reward/compose.py` 与 `src/evidenceweaver/reward/__init__.py`。
- 结果：仓库现在不仅能在 evaluator 路径里生成 reward bundle，也可以通过独立的 reward CLI 对 task + run artifact 进行 reward 组合与说明输出，为后续独立 reward pipeline 打下基础。

04月21日00时42分
- 工作：扩展 evidence graph 的 claim-to-claim 关系边，目前在 `graph/core.py` 中加入了 `derived_from` 与 `duplicates` 的启发式生成逻辑。
- 结果：evidence graph 不再只有 source-to-claim 的 `supports` 边，已经开始具有更丰富的结构语义，这对未来做 graph-level diagnostics 和 reward shaping 很重要。

04月21日00时42分
- 工作：新增 `tests/test_reward_graph.py`，覆盖：
  - reward module 与 baseline reward bundle 的一致性
  - graph 中存在非 `supports` 的关系边
  - reward CLI 可以输出 reward bundle 与 notes
- 结果：当前测试已经从 agent/evaluator 扩展到 graph semantics 与独立 reward 模块。

04月21日00时42分
- 工作：更新 `README.md`、`docs/interfaces.md`、`paper/draft.md`，把 reward module、claim-to-claim graph edges 与新的 CLI 入口写入文档。
- 结果：文档与代码继续保持同步，没有出现“实现变了、文档没跟上”的情况。

04月21日00时42分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.reward.compose benchmarks/snapshot_v0/tasks/agent_training_stack_task.json /tmp/evidenceweaver_reward_test_run.json`
- 验证结果：
  - 14 个单元测试全部通过。
  - reward CLI 已经可以稳定输出 `reward_bundle` 与 `notes`。
  - graph 中已能观察到 `derived_from` / `duplicates` 这类非 `supports` 边。

=== 更新后的下一步计划（04月21日00时42分） ===

1. 继续丰富 evidence graph 语义，逐步把 `contradicts` 等更强关系补进来。
2. 把 reward pipeline 与 evaluator 进一步解耦，形成更清晰的 reward-generation contract。
3. 为 benchmark task 增加更系统的 provenance 和 refresh metadata。
4. 把 diagnostics 从 run artifact 扩成更丰富的 debug / analysis surface。
5. 在 trainer 选型前，继续扩大 baseline、graph、reward、benchmark 四者之间的实验合同。

04月21日00时46分
- 工作：新增 graph analysis CLI：`src/evidenceweaver/graph/analyze.py`，可以直接读取 run artifact 并输出 graph summary，包括 edge 统计、opened source、focus coverage ratio、open questions、diagnostics 与 reward bundle。
- 结果：仓库现在不仅能生成 graph，还能把 graph 单独分析出来，开始具备更像 debug / analysis surface 的能力。

04月21日00时46分
- 工作：继续补 README 与接口文档，把 `evidenceweaver.graph.analyze` CLI 和 graph analysis 能力写进公开文档。
- 结果：当前 GitHub 首页和接口文档已经能反映“agent -> graph -> reward -> analysis”这一更完整的 artifact 流程。

04月21日00时46分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
- 验证结果：
  - 15 个单元测试全部通过。
  - graph analysis CLI 已被测试覆盖，可正常输出 summary。

=== 更新后的下一步计划（04月21日00时46分） ===

1. 把 `contradicts` 等更强 graph 关系引入 evidence graph。
2. 把 graph analysis CLI 扩成更完整的 debug report / sidecar artifact。
3. 继续扩展 reward module，让 reward notes / reward generation 更系统化。
4. 为 benchmark 加更规范的 provenance / refresh metadata。
5. 在继续引入 trainer 之前，把 graph、reward、benchmark、analysis 这四层再打牢一些。

04月21日09时39分
- 工作：把项目正式推进到“真实样例驱动的精度优化”阶段，新增 `benchmarks/real_cases_v1/`，形成五任务 suite，并新增 `src/evidenceweaver/optimize/accuracy.py` 来做 baseline 配置 sweep。
- 结果：仓库已经不只是能跑 benchmark，而是能对 baseline 配置做多任务比较和简单优化，为后续更系统的精度提升打下基础。

04月21日09时39分
- 工作：新增两个更接近真实应用的任务：
  - `computer_use_rl_task`
  - `swe_agent_posttraining_task`
  同时保留 stability / training stack / deep search 三个原有任务，组成五任务 `real_cases_v1` suite。
- 结果：当前 benchmark 已经覆盖 agentic RL 稳定性、训练基础设施、deep search、computer use、software engineering 五类任务，不再只有单一主题。

04月21日09时39分
- 工作：对 baseline 做了多配置 sweep，并把结果保存到 `benchmarks/real_cases_v1/results/baseline_sweep.json`。
- 结果：当前默认 baseline 配置已经和 sweep 最优配置对齐；当前 suite 最优平均 `overall_score` 约为 `0.8283`。

04月21日09时39分
- 工作：从 sweep 结果中识别出了当前 hardest case：`swe-agent-posttraining-task` 明显弱于其他任务。
- 结果：仓库现在不仅有“平均变好了”的信号，还有“下一步该针对哪类真实任务做精度优化”的明确方向。

04月21日09时39分
- 工作：更新 `README.md`、`docs/benchmark-slice.md`、`docs/interfaces.md`、`paper/draft.md`，写入 `real_cases_v1` 和 optimizer 的存在、当前最优配置、当前 hardest task 等信息。
- 结果：项目叙事已经升级到“真实样例驱动的调优阶段”，而不仅是基础 scaffold 阶段。

04月21日09时39分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.optimize.accuracy benchmarks/real_cases_v1/tasks`
- 验证结果：
  - 18 个单元测试全部通过。
  - optimizer CLI 可正常输出多配置排名和最佳配置。
  - 当前 best config 的五任务平均分约为 `0.8283`。

=== 更新后的下一步计划（04月21日09时39分） ===

1. 重点针对 `swe-agent-posttraining-task` 做定向精度优化，因为它现在是最明显短板。
2. 引入更细粒度的 benchmark provenance / metadata，让真实样例的更新与复查更标准化。
3. 让 optimizer 输出更丰富的 per-task failure summary，而不只是聚合分数。
4. 考虑在 baseline 中加入 task-family-aware heuristic，而不是所有任务一套规则。
5. 在继续扩大 benchmark 之前，先把真实样例驱动的误差分析能力补强。

04月21日09时45分
- 工作：为 `real_cases_v1` 全部五个任务引入 `provenance` 字段，并扩展 `TaskBundle` / `task-bundle.v0` 支持该元数据结构。
- 结果：benchmark artifact 不再只是题目和文档本身，而是开始具备来源治理能力；每个任务现在都能携带 `snapshot_style`、`primary_urls`、`refresh_hint` 和 `notes`。

04月21日09时45分
- 工作：把 provenance 能力补进 README、benchmark README、接口文档，并增加测试确保 real-cases suite 的任务都带 provenance。
- 结果：项目已经从“benchmark 存储”继续推进到“benchmark 治理”，后续扩展真实样例时更容易维持来源一致性和刷新策略。

04月21日09时45分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - 对 `benchmarks/real_cases_v1/tasks/*.json` 逐个检查 `provenance.primary_urls`
- 验证结果：
  - 18 个单元测试全部通过。
  - real-cases suite 全部任务都已携带 provenance。

=== 更新后的下一步计划（04月21日09时45分） ===

1. 继续针对 `swe-agent-posttraining-task` 做专项精度优化，因为它仍然是当前最弱项。
2. 把 optimizer 扩展成能输出更明确的 failure summary，而不仅是 aggregate 分数。
3. 在 graph 里继续引入 `contradicts` 等更强关系语义。
4. 让 provenance 后续能支持 refresh recipe 或 source snapshot versioning。
5. 继续把“真实样例驱动的误差分析”做成项目主线，而不是只停留在一次性 sweep。

04月21日09时50分
- 工作：针对 `swe-agent-posttraining-task` 做了专项精度优化，主要包括：
  - baseline sentence selection 支持相邻句拼接 candidate
  - 增强与 SWE 相关的 signal fragments
  - 调整 SWE task 的 paraphrased digest 使其与 claim 更一致且更接近原始意图
- 结果：SWE 任务从此前明显的短板变成当前 suite 中不再最弱的一项，real-cases 优化结果整体显著提升。

04月21日09时50分
- 工作：重新运行 `real_cases_v1` optimizer，并把新结果写回 `benchmarks/real_cases_v1/results/baseline_sweep.json`。
- 结果：当前 best average `overall_score` 已提升到约 `0.9683`，当前 hardest task 已从 SWE 切换为 `computer-use-rl-task`。

04月21日09时50分
- 工作：更新 README、benchmark 文档与 paper draft 中关于 real-case 优化结果的叙述。
- 结果：仓库公开叙事已经同步反映“通过真实样例驱动优化后，SWE 弱点已被明显改善”的新状态。

04月21日09时50分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.optimize.accuracy benchmarks/real_cases_v1/tasks`
- 验证结果：
  - 18 个单元测试全部通过。
  - optimizer 结果显示：
    - best average `overall_score` 约为 `0.9683`
    - current hardest task 为 `computer-use-rl-task`
    - current hardest-task score 约为 `0.925`

=== 更新后的下一步计划（04月21日09时50分） ===

1. 继续针对 `computer-use-rl-task` 做专项精度优化，把当前最弱项继续往上抬。
2. 给 optimizer 增加更清晰的 failure summary 与 per-task recommendation，而不只是排名。
3. 继续推进 `contradicts` 等更强 graph 关系，为下一轮错误分析做准备。
4. 把 provenance 从静态 metadata 逐步推进到更明确的 refresh / versioning contract。
5. 保持真实样例驱动迭代：每次发现最弱项，就针对该类任务做定向优化并回写 sweep 结果。

04月21日09时55分
- 工作：把去重策略从全局规则改成 task-family-aware 规则，只在 `computer-use` 和 `software engineering` 这两类任务上启用更强的重复 claim 抑制，而不影响 stability / deep-search 等任务的探索性表达。
- 结果：避免了“修好一个任务、拖坏另外几个任务”的副作用，real-cases suite 的总体平均分进一步提升。

04月21日09时55分
- 工作：重新运行 `real_cases_v1` optimizer 并回写新的 `baseline_sweep.json`。
- 结果：当前 best average `overall_score` 约为 `0.9733`，五任务当前表现为：
  - `agent-training-stack-task`: `1.0`
  - `agentic-rl-stability-task`: `0.95`
  - `computer-use-rl-task`: `0.95`
  - `deep-search-reward-task`: `0.9667`
  - `swe-agent-posttraining-task`: `1.0`

04月21日09时55分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.optimize.accuracy benchmarks/real_cases_v1/tasks`
- 验证结果：
  - 18 个单元测试全部通过。
  - best average `overall_score` 提升到约 `0.9733`。
  - 当前 weakest task 仍然是 `computer-use-rl-task`，但已提升到约 `0.95`。

04月21日11时20分
- 工作：给 optimizer 增加 `failure_summary`，现在每个 candidate config 的结果都会包含 weakest task、weakest score 和下一步建议。
- 结果：真实样例驱动阶段不再只是“跑分排序”，而是开始能自动指出下一轮优化方向。

04月21日11时20分
- 工作：将带 `failure_summary` 的 optimizer 结果重新写入 `benchmarks/real_cases_v1/results/baseline_sweep.json`，并更新 README / benchmark 文档。
- 结果：保存的 sweep artifact 现在可直接作为误差分析输入，后续 agent 接管时不需要重新运行才能知道当前弱点和建议。

04月21日11时20分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.optimize.accuracy benchmarks/real_cases_v1/tasks`
- 验证结果：
  - 18 个单元测试全部通过。
  - optimizer 输出现在包含 `failure_summary`。

=== 更新后的下一步计划（04月21日11时20分） ===

1. 把 failure summary 从启发式建议进一步升级成 per-claim / per-edge 误差解释。
2. 为 computer-use task 继续做 precision 提升，尝试把当前 `0.95` 推到更高。
3. 增加新的真实样例任务，防止当前五任务 suite 被过拟合。
4. 利用已授权的公开网络抓取，补更真实的 public-source snapshot seed。
5. 继续保持每次优化都回写 `baseline_sweep.json` 和 `codex_work`。

04月21日11时36分
- 工作：利用公开来源继续扩展 `real_cases_v1`，新增三类真实样例任务：
  - `terminal_agent_rl_task`
  - `browser_agent_environment_task`
  - `tool_user_interaction_task`
- 结果：real-cases suite 从 5 个任务扩展到 8 个任务，覆盖范围进一步扩展到 terminal agents、browser/web agents、tool-agent-user evaluation。

04月21日11时36分
- 工作：新增任务后按“最弱项优先”继续优化：
  - `tool-user-interaction-task` 从约 `0.425` 提到约 `0.95`
  - `browser-agent-environment-task` 从约 `0.5917` 提到 `1.0`
  - `terminal-agent-rl-task` 从约 `0.6292` 提到约 `0.9625`
- 结果：扩展后的 8 任务 suite 当前 best average `overall_score` 约为 `0.9724`，所有任务当前都 >= `0.95`。

04月21日11时36分
- 工作：更新 `baseline_sweep.json`、README、benchmark 文档，并保留 optimizer failure summary。
- 结果：仓库已经从 5 任务调优进入 8 任务真实样例调优阶段，且有保存的 sweep artifact 可以复核。

04月21日11时36分
- 验证：
  - `PYTHONPATH=src python3 -m unittest discover -s tests -v`
  - `PYTHONPATH=src python3 -m evidenceweaver.optimize.accuracy benchmarks/real_cases_v1/tasks`
- 验证结果：
  - 18 个单元测试全部通过。
  - 扩展 suite 当前 best average `overall_score` 约为 `0.9724`。
  - 当前所有 real-case tasks 均 >= `0.95`。

=== 更新后的下一步计划（04月21日11时36分） ===

1. 继续添加更多真实样例任务，防止 8 任务 suite 被过拟合。
2. 将 optimizer 的 failure summary 进一步细化到 per-claim / per-source 级别。
3. 开始为 benchmark 任务生成更完整的 source snapshot sidecar，而不只是在任务 JSON 中放 paraphrased digest。
4. 继续补 `contradicts` 等 graph 关系，并让 graph analysis report 更适合调试。
5. 将 paper draft 中的实验章节改成和当前 real-case sweep 更一致的结构。

04月21日11时48分
- 工作：根据用户要求，把当前项目进度和未来优化路线整理成 GitHub 可读文档 `docs/progress-and-roadmap.md`，并在 README 中加入入口。
- 结果：远端仓库现在有单独文档记录：当前阶段、已实现组件、benchmark 状态、最新 real-case 分数、已做的优化、已知限制和下一步路线。

04月21日11时48分
- 当前阶段总结：
  - 项目已经进入 real-example-driven accuracy optimization 阶段。
  - 当前闭环为 `task bundle -> baseline agent -> evidence graph -> diagnostics -> reward bundle -> graph analysis -> optimizer sweep`。
  - `real_cases_v1` 当前有 8 个公开来源驱动任务。
  - 当前 best average `overall_score` 约为 `0.9724`。
  - 当前所有 real-case tasks 都 >= `0.95`。

04月21日11时48分
- 未来优化路线已写入 `docs/progress-and-roadmap.md`：
  1. 扩展更难真实任务，避免过拟合当前 8 题。
  2. 增加 source snapshot sidecars。
  3. 将 failure summary 细化到 per-claim / per-source。
  4. 深化 evidence graph 关系语义，如 `contradicts` / `requires` / `resolves_open_question`。
  5. 进一步解耦 reward 和 evaluator。
  6. 增加 trainer-readiness artifacts。

=== 更新后的下一步计划（04月21日11时48分） ===

1. 下一轮优先增加 source snapshot sidecar 或更难的 real-case tasks。
2. 如果继续优化当前 suite，必须先增加任务数量，避免过拟合。
3. 后续每次 benchmark 或 baseline 变化都要更新 `baseline_sweep.json`、`docs/progress-and-roadmap.md` 和 `codex_work`。
